- name: Create All Roboshop Parameters
  hosts: localhost
  tasks:
    - name: Create Value
      community.aws.ssm_parameter:
       name: "param.dev.catalogue.MONGO"
       value: "xyz"
       region: "us-east-1"
